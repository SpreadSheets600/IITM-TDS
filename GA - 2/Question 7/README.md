# Question 7

This directory is used to trigger the caching workflow for CI and to test your understanding of how caching works. You do not need to edit any files here, but you can look at the workflow file `.github/workflows/cache_test.yml` to understand how it works. The workflow will run whenever you push commits to this directory, and it will print out whether the cache was hit or missed. Try pushing some commits here and see the results!
