# Question 3

## Reconstructed Question
Build a valid **dbt staging model** SQL for customer/order analytics using the provided raw source table, including standardized column names and appropriate type handling.

## Reasoning
This question expects a dbt SQL model artifact rather than a computed numeric answer. The prepared SQL in `answer.sql` follows the required dbt model structure and transformation intent.

## Files
- `answer.sql` - final SQL to submit.

## How to use
Submit the SQL content from `answer.sql` in the quiz answer box.
